# Gemini (text + embeddings) — ADR-001
GEMINI_API_KEY=

# Supabase (pgvector + storage) — use service role only on the server / cron
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: Vercel Cron sends Authorization: Bearer <CRON_SECRET>
CRON_SECRET=
